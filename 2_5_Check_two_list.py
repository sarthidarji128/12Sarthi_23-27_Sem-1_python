# Create two dictionaries with the same key-value pairs
dict1 = {"name": "sarthi", "age": 17}
dict2 = {"name": "sarthi", "age": 17}

# Compare the dictionaries using the `==` operator
result = dict1 == dict2  # Check if they have the same key-value pairs (order doesn't matter)

# Print the result of the comparison
print("Result:", result)
